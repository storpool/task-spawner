apiVersion: v1
kind: Service
metadata:
  name: ticket-spawner
  labels:
    app: ticket-spawner
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
  selector:
    app: ticket-spawner